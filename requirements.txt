# Food Log API — Python dependencies
# Install with:  pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # required for file uploads
pydantic>=2.7.0
httpx>=0.27.0               # required by FastAPI TestClient
pytest>=8.2.0               # optional — for running tests with pytest
anthropic>=0.40.0           # Claude API — recipe extraction from URL/image
beautifulsoup4>=4.12.0       # HTML parsing for recipe-from-URL extraction
curl_cffi>=0.7.0             # browser-TLS-fingerprint HTTP client — bypasses bot detection on recipe sites
